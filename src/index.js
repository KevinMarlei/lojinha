import React from 'react';
import ReactDOM from 'react-dom/client';
import Caminho from './rotas/Rotas';
import './index.css'


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Caminho/>
  </React.StrictMode>
);

